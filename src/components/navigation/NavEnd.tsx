import React from "react";
import { Toolbar } from "@mui/material";
import { NavBarContacts } from ".";

export const NavEnd = () => {
  return (
    <Toolbar>
      <NavBarContacts />
    </Toolbar>
  );
};
