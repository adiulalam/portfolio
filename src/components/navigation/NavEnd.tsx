import React from "react";
import { Toolbar } from "@mui/material";
import { NavMenuContacts } from ".";

export const NavEnd = () => {
  return (
    <Toolbar>
      <NavMenuContacts />
    </Toolbar>
  );
};
